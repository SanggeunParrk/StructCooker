# Submit Layout

- `ingest/`, `filters/`, `metadata/`, `exports/`, `search/`, `analysis/`: first-class cluster entrypoints that map to current configs
- `maintenance/`: utilities that operate on existing datasets but are not part of the main workflow surface
- `legacy/`: environment-specific or historical scripts kept only for reference during migration
