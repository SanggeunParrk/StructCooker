data_dir: ${p:/public_data/CCD/components/}
file_pattern: "*.cif*"

env_path: ${p:/public_data/CCD/biomol_CCD_202602.lmdb}
recipe: ${p:pipelines/recipe/ccd_recipe_book.py}


n_jobs: -1

reader:
  loader: pipelines.transforms.cif_transforms.get_cif_data

writer:
  serializer: pipelines.utils.convert.to_bytes
